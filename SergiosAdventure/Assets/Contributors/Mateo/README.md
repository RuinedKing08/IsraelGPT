Mateo

- Runtime narrativo original y referencia del `GayManager` legacy en `Legacy/`.
- Editor del grafo narrativo preservado en `Editor/GraphToolkit/`.
- El runtime activo final se integro en `Assets/Scripts/Story/StoryGameManager.cs`.
